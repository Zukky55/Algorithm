#if 0
#include <iostream>
#include <random>
using namespace std;

//#define ARRAY_LENGTH(array) (sizeof(array) / sizeof(array[0]))

const int ARRAY_SIZE = 9;
int a[ARRAY_SIZE] = { 4,8,7,5,1,3,9,0,2 };
int heap[ARRAY_SIZE];

void  Merge() {
}

void Split(int val[], size_t array_size)
{
	for (int i = 0; i < array_size; ++i)
	{
	}





	//cout << ARRAY_LENGTH(val);
	//int count;
	//for (int i = 0; i < ARRAY_LENGTH(val); i++)
	//{
	//	count++;
	//	cout << count << endl;
	//}
}

int main()
{
	//memcpy(heap, a, sizeof(a));
	Split(a, sizeof(a) / sizeof(int));
	Merge();
}
#endif // 1
